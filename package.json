{
  "name": "stack",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.3"
  },

  "scripts": {
    "dev": "ts-node-dev --transpile-only --respawn --ignore-watch node_modules index.ts"
  }
}
